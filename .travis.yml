language: python
python:
  - "3.6"
git:
  depth: false
jobs:
  include:
    - install:
        - pip install -r requirements.txt
        - pip install codecov
        - pip install coveralls
      script: coverage run -m unittest
      after_success:
        - codecov
        - coveralls
